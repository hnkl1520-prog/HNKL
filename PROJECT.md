# HNKL Editor — 프로젝트 설명

> 포트폴리오 사이트와, 그 사이트를 손으로 다듬기 위해 직접 만든 편집 도구.
> 2025년 12월 시작, 65개 커밋. 에디터 약 9,200줄 / 사이트 약 12,500줄.

---

## 1. 배경 — 왜 만들었나

### 문제

AI로 코딩하는 사람(이른바 '바이브 코딩')에게는 두 가지 비용이 계속 발생한다.

**첫째, 미세조정의 비용.** 마진 하나가 마음에 안 들 때마다 AI에게 설명하고, 고쳐진 코드를 확인하고, 또 어긋나면 다시 설명한다. 값 하나를 8px 옮기는 데 시간과 크레딧이 든다. 눈으로 보면 2초에 끝날 판단인데, 말로 옮기는 과정에서 대부분의 비용이 발생한다.

**둘째, 구조 잡기의 비용.** 페이지의 뼈대를 AI에게 맡기면 의도와 다른 구조가 나오고, 그걸 고치는 대화가 다시 길어진다. 차라리 구조는 손으로 잡고 그 코드를 AI에게 넘기는 편이 빠르다.

### 이 도구의 자리

**"AI에게 시키기엔 아까운 일"과 "손으로 하기엔 번거로운 일" 사이.**

- 워드프레스·Wix 같은 CMS: 코드를 내주지 않는다. 결과물이 그들의 형식에 갇힌다.
- Figma: 디자인은 되지만 실제 코드가 아니다.
- 개발자도구: 고칠 수는 있지만 새로고침하면 사라진다.

이 도구는 **실제 HTML 파일을 화면에서 고치고 그대로 저장한다.** 결과물은 평범한 정적 사이트이고, 그 코드를 다시 AI에게 넘길 수 있다.

### 대상

**디자이너.** 개발자가 아니다. 이 전제가 도구의 거의 모든 결정을 지배한다 —
`max-width`, `calc(820px * var(--vb-s))`, CSS 선택자, `!important` 같은 것이 화면에 보이면 실패로 본다.

---

## 2. 목적

### 핵심 원칙: 디자인 시스템 안에서 움직인다

색·글자 크기·간격을 **임의의 값으로 입력하지 않고 등록된 토큰에서 고른다.** 이것이 이 도구가 CMS나 개발자도구와 갈리는 지점이다.

다만 이 원칙은 한 번 완화되었다. 초기에는 시스템 눈금(4·8·12·16·24·32·48·64·96·128·160·192px)을 벗어난 값을 주황색으로 경고했는데, `clamp()`로 화면 폭에 따라 흐르는 값이나 디자인상 필요한 중간값은 눈금에 없다. **시스템대로 쓰는 자리까지 나무라는 상태**가 되어 경고를 걷어냈다. 대신 끌 때 12px 안으로 들어오면 자석처럼 붙는 것만 남겼다 — 도움은 되되 강요하지 않는다.

### 세 가지 목표

1. **미세조정을 화면에서** — 간격을 끌어서 맞추고, 결과를 즉시 본다
2. **구조를 손으로** — 컴포넌트를 끌어다 놓아 페이지를 짜고, 그 코드를 AI에게 넘긴다
3. **결과물은 평범한 코드** — 도구에 종속되지 않는다

---

## 3. 구조

```
HNKL/
├── public/                     사이트 (결과물)
│   ├── tokens.css              디자인 시스템 (97개 토큰)
│   ├── common.css / common.js  공통 스타일·스크립트
│   ├── index / about / works   주요 페이지
│   └── works/
│       ├── projects/           6개 프로젝트 (vibra, capus, korail, restmode, swig, vaplay)
│       └── labs/               9개 실험물
│
└── editor/                     편집 도구
    ├── server.js               파일을 읽고 고쳐 쓰는 로컬 서버 (:5180)
    ├── static.js               에디터 없이 사이트만 보는 서버 (:8000)
    ├── editor.config.json      어느 사이트를 다루는지 (경로 설정)
    ├── lib/
    │   ├── patch.js            HTML 을 외과적으로 고치는 엔진
    │   ├── designsystem.js     tokens.css 를 읽어 팔레트·타이포로 정리
    │   └── config.js           경로 설정 로더
    ├── app/                    에디터 화면 (app.js / app.css / index.html)
    ├── inject/bridge.js        미리보기 안에서 도는 스크립트
    └── icons/300/              Material Symbols wght300
```

### 두 화면

**Edit** — 왼쪽 컴포넌트 라이브러리, 가운데 실제 페이지 미리보기, 오른쪽 인스펙터.
**Design System** — 팔레트·역할·글꼴·타이포 눈금을 보고 고친다. 여기서 바꾼 값은 `tokens.css`에 저장되어 사이트 전체에 반영된다.

---

## 4. 핵심 기술 결정

### parse5 + sourceCodeLocationInfo — 건드린 줄만 바뀐다

가장 중요한 결정. HTML을 파싱해 다시 문자열로 뱉으면 들여쓰기·주석·따옴표 스타일이 전부 재작성되어 **git diff가 파일 전체로 부풀어 오른다.**

대신 parse5의 `sourceCodeLocationInfo`로 각 요소가 원본 문자열의 몇 번째 글자에 있는지 알아내고, **그 구간의 글자만 갈아 끼운다.** 나머지는 원본 그대로다. 그래서 값 하나를 고치면 diff에도 그 한 줄만 나온다.

이 위에 14개의 편집 연산이 올라간다:

```
patchInlineStyle   요소의 style="" 수정
patchCssRule       CSS 규칙 자체 수정 (같은 클래스 전부에 반영)
patchAttr          속성 수정 (src, class, data-* 등)
patchText          글자 수정
insertHtml         요소 삽입
replaceElement     요소를 다른 태그로 교체 (사진 ↔ 영상)
removeElement      삭제
duplicateElement   복제
moveElement        위아래 이동
appendCss          CSS 규칙 추가
applyMotion        인터랙션 클래스 + CSS 부착
linkAsset          <head> 에 링크 삽입
```

`replaceElement`가 필요했던 이유: 사진 자리에 영상을 넣으면 `<img>`를 `<video>`로 바꿔야 하는데, `src`만 바꾸면 재생 안 되는 `<img>`가 남고, 지웠다 다시 넣으면 뒤따르는 형제들의 경로가 밀린다.

### postMessage 다리 — 에디터와 미리보기

미리보기는 iframe 안의 진짜 페이지다. 에디터와 iframe은 `postMessage`로 대화한다.

- 미리보기 → 에디터: 무엇을 골랐는지, 계산된 값이 얼마인지, 페이지 높이
- 에디터 → 미리보기: 값 미리보기, 여백 강조, 요소 교체, 스크롤 이동

**저장 전에는 파일을 건드리지 않는다.** 바꾼 값은 임시 `<style>` 태그와 인라인 스타일로 화면에만 반영되고, `저장`을 눌러야 파일에 쓴다. 그때까지는 `Changed 3` 같은 표시로 몇 개가 대기 중인지 보여주고, `⌘Z`로 하나씩 되돌릴 수 있다.

### 설정으로 경로 분리

`editor.config.json`에 `siteRoot`, `tokensFile`, `sharedCss` 등을 적어 둔다. 코드에 경로가 박혀 있지 않아, 다른 구조의 사이트에도 이 파일만 고쳐 붙일 수 있다.

---

## 5. 기능

### 5.1 간격 조절 — 이 도구의 주인공

**여백 판(box model).** 고른 요소의 `Outside`(margin)와 `Inside`(padding)를 안팎 두 겹의 상자로 보여준다. 숫자를 세로로 끌면 값이 변하고, 미리보기에서 그 자리가 색으로 밝아진다. 손을 떼면 가까운 시스템 단계에 붙는다(`Alt`로 해제).

**카드 사이 간격(`Between`).** flex·grid 묶음이면 `gap`을 여백 판 바로 아래에 둔다. 원래는 `Layout` 접힘 안에 묻혀 있었는데, 이 도구의 주인공이 간격인데 두 번 눌러야 닿는 자리에 있었다.

**페이지 전체 간격.** 아무것도 고르지 않으면 인스펙터가 페이지 설정으로 바뀐다 — `Content width`, `Section spacing`. 여기서 한 값을 바꾸면 모든 섹션이 함께 움직인다. 이 값을 따르지 않는 섹션은 `2 sections set their own`으로 조용히 알리고, 손을 올리면 미리보기에서 그 섹션들이 밝아진다.

**여럿 고르기.** `Shift`로 같은 위계의 요소 여럿을 고르면 그 사이 간격들이 뜬다.

### 5.2 컴포넌트

11개 블록을 세 갈래로 나눠 왼쪽 패널에 둔다.

| 갈래 | 항목 |
|---|---|
| TEXT | Section header, Title + text, Group header |
| MEDIA | Image, Video |
| LAYOUT | Project info, Credit logos, Image + text, Media carousel, 3-column cards, Interaction grid |

- 끌어다 놓으면 실제 HTML로 들어간다
- 카드를 끌어 **순서를 바꾸고 갈래도 옮길 수 있다** (자동 저장)
- 갈래 제목을 눌러 접는다
- 페이지에서 고른 덩어리를 **직접 컴포넌트로 등록**할 수 있다. 이때 그 덩어리가 쓰는 CSS도 함께 저장되어, 다른 페이지에 놓아도 모양이 깨지지 않는다

### 5.3 캐러셀 — 처음부터 끝까지 손으로

가장 복잡한 컴포넌트라, 이 도구가 어디까지 할 수 있는지 보여주는 예다.

```
① 끌어다 놓기 → "몇 장?" (화면 한가운데 뜨는 창, +/− 로 조절)
② 카드를 클릭 → 인스펙터에서 미디어를 넣는다
     media/ 폴더 목록이 입력칸에 자동완성으로 뜬다
     영상 주소를 넣으면 <img> 가 <video> 로 바뀐다 (재생버튼 자동)
③ 트랙을 클릭 → Carousel 설정
     Photo card  2.2 sec
     Video card  5.0 sec    ← 영상은 재생 시간을 벌어야 한다
④ 오른쪽 끝 + → 카드 한 장 더
```

자동넘김 속도를 위해 사이트 코드도 고쳤다. `currentDelay()`가 2200/5000을 박아 두고 있어서 에디터가 손댈 데가 없었다 — 트랙의 `data-delay`를 먼저 보게 바꿨다.

### 5.4 인터랙션

끌어다 놓으면 클래스와 CSS가 함께 붙는다.

| | |
|---|---|
| Lift on hover | 살짝 떠오르고 그림자 |
| Grow on hover | 1.04배로 커짐 |
| Press on click | `scale(.96)` 으로 눌렸다 돌아옴 |
| Reveal on scroll | 아래에서 올라오며 나타남 |

### 5.5 디자인 시스템 화면

`tokens.css`를 읽어 네 섹션으로 보여준다.

- **Palette** — Gray(14) · Primary(10) · Secondary 1(5) · Secondary 2(5) · Black & white
- **Roles** — 페이지 배경, 본문 글자, 선·테두리 같은 역할에 어느 색을 쓸지. **LIGHT / DARK 두 벌을 좌우로** 놓아 같은 역할이 가로로 마주 본다
- **Font** — 사이트가 쓰는 글꼴과 실제로 쓰이는 굵기. 글꼴 이름은 훑은 CSS에서 찾으므로 다른 사이트에 붙여도 맞는다
- **Type** — 크기 눈금과 각 크기가 어디에 쓰이는지

여기서 바꾼 값은 `tokens.css`에 저장되고, 저장 전에 어떤 값이 어떻게 바뀌는지 확인 창을 띄운다.

### 5.6 그 밖

- **미리보기에서 글자 직접 고치기** (더블클릭)
- **섹션 순서 바꾸기** — 버튼과 드래그 둘 다
- **뷰포트 전환** — 데스크탑 / 노트북 / 태블릿 / 폰
- **되돌리기 / 다시하기** (`⌘Z`, `⌘⇧Z`)
- **실제 브라우저로 열기** — 편집 도구 없는 진짜 페이지
- **앱으로 설치** (PWA)

---

## 6. 이 도구를 만들며 배운 것

### "값이 안 먹는다"의 세 가지 정체

같은 증상인데 원인이 다르고, 도구가 각각을 설명해야 했다.

1. **`max-width`가 막고 있다** — 폭을 늘려도 상한선에 걸린다
2. **자식이 제 값을 갖고 있다** — 부모의 정렬을 자식이 덮어쓴다
3. **여백이 겹친다(margin collapse)** — 위아래 마진은 큰 쪽만 살아남는다

### 이미지 크기는 %로 밀면 안 된다

이미지는 틀에 꽉 차게(`object-fit: cover`) 들어간다. 그래서 **폭을 75%로 줄여도 크기가 아니라 잘리는 자리만 바뀐다.** `Shape`(16:9 / 4:3 / 1:1 / 3:4)로 틀의 비율을 고르는 쪽이 맞다.

### 개발자 언어를 걷어내는 일은 한 번에 안 끝난다

`Max width`, `Whole rule / This element`, `MARGIN / PADDING`, `.vb-title, h2` 같은 선택자 나열, `save to write it to the file` — 이런 것들이 계속 나왔다. 크기가 토큰이 아니라 `style="font-size: 2.25rem"`으로 제자리에 박혀 있는 곳도 많아, 속성 선택자까지 동원해야 했다.

### 세로 간격이 세 겹으로 쌓여 있었다

사이트 쪽 문제. 여덟 개의 값이 서로를 덮어쓰고 있어서 "군데군데 간격이 이상한" 원인이 됐다. 두 개의 토큰으로 모으고, 시각적으로 무거운 카드·캐러셀 뒤에 오는 네 자리만 `--lg` 클래스로 구분했다.

---

## 7. 현재 상태와 남은 것

**동작하는 것.** 위에 적은 기능 전부. 테스트 61개(패치 엔진 28 + 실제 파일 33) 통과.

**남은 것**

- 왼쪽 패널 카드의 썸네일이 아직 납작한 도형이다. 실제 모양 미리보기로 바꾸면 좋다
- `play` · `pause` · `stop` 아이콘이 아직 안 쓰인다. 캐러셀 자동넘김 켜고 끄는 버튼에 맞을 것 같다
- 히어로 섹션을 마스터 블록으로 만드는 구조는 짜 두었으나 실제로 써보지 않았다. HTML·CSS는 옮길 수 있지만 JS는 자동 추출이 안 된다
- 배포를 고려하면 아직 HNKL 고유의 가정이 남아 있다 — `designsystem.js`의 토큰 이름 규칙, 내장 컴포넌트 11개

---

## 8. 한 줄로

**디자인 시스템 안에서, 화면을 보면서, 실제 코드를 고치는 도구.**
결과물은 평범한 정적 사이트여서 다시 AI에게 넘길 수 있다.
